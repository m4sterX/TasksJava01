package by.htp.nktTrvts;

public class N17 {
    public static void main(String[] args) {
        int a = 25;
        int b = -35;
        double midA = (Math.pow(a,3) + Math.pow(b,3)) / 2;
        double midG = (Math.sqrt(Math.abs(a)) * Math.abs(b));
    }
}
