package by.htp.nktTrvts;

public class N29 {
    public static void main(String[] args) {
        int a = 6;
        int b = 10;
        int c = 3;
        double cA = (Math.pow(a,2) + Math.pow(c,2) - Math.pow(b,2)) / 2 * a * c;
        double cB = (Math.pow(a,2) + Math.pow(b,2) - Math.pow(c,2)) / 2 * a * b;
        double cC = (Math.pow(b,2) + Math.pow(c,2) - Math.pow(1,2)) / 2 * b * c;
    }
}
